workflow Test {
  input {
    cwd: path
  }
  policy {
    deny fs.write(path) if path.starts_with("x")
  }
  stage@entry Start {
    prompt: "start"
    output: {
      x: string
    }
  }
  stage@exit Fin {
    prompt: "done"
    input: Start.x
    output: {
      summary: string
    }
  }
}
