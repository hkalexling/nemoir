workflow Test {
  input {
    x: string
  }
  policy {
    deny fs.read(path) if not unknown_input.contains(path)
  }
  stage@entry Start {
    prompt: "start"
    output: {
      ok: bool {
        true => Fin
        false => Start
      }
    }
  }
  stage@exit Fin {
    prompt: "done"
    input: Start.ok
    output: {
      done: string
    }
  }
}
