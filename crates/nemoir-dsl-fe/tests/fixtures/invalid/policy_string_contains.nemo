workflow Test {
  input {
    name: string
  }
  policy {
    deny fs.read(path) if not name.contains(path)
  }
  stage@entry Start {
    prompt: "start"
    output: {
      x: string
    }
  }
  stage@exit Fin {
    prompt: "done"
    input: Start.x
    output: {
      summary: string
    }
  }
}
