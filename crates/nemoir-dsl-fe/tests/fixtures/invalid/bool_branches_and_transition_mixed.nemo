workflow MixedBoolAndTransition {
  stage@entry A {
    prompt: "start"
    output: {
      ok: bool { true => B false => B }
    }
    transition if ok => B
  }
  stage@exit B {
    prompt: "done"
  }
}
