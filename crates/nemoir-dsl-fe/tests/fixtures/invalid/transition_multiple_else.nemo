workflow MultipleElse {
  input { eps: number }
  stage@entry S {
    prompt: "x"
    output: { score: number }
    transition else => A
    transition else => B
  }
  stage@exit A { prompt: "a" }
  stage@exit B { prompt: "b" }
}
