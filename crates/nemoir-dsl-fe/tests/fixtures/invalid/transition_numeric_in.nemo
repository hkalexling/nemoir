workflow TransitionNumericIn {
  stage@entry A {
    prompt: "start"
    output: {
      score: number?
    }
    // Numeric `in [...]` in a transition guard is not supported (plan §3.4).
    transition if score in [1] => B
  }
  stage@exit B {
    prompt: "done"
  }
}
