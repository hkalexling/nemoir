workflow Test {
  input {
    x: string
  }
  policy {
    before fs.write(path) requires fs.read(filename)
  }
  stage@entry Start {
    prompt: "start"
    output: {
      ok: bool {
        true => Fin
        false => Start
      }
    }
  }
  stage@exit Fin {
    prompt: "done"
    input: Start.ok
    output: {
      done: string
    }
  }
}
