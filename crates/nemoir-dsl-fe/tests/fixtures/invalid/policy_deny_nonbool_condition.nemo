workflow Test {
  input {
    cwd: path
  }
  policy {
    deny fs.read(path) if cwd
  }
  stage@entry Start {
    prompt: "start"
    output: {
      x: string
    }
  }
  stage@exit Fin {
    prompt: "done"
    input: Start.x
    output: {
      summary: string
    }
  }
}
