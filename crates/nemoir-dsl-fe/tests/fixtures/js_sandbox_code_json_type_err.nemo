workflow JsSandboxCodeJsonType {
  input {
    code: json
  }

  policy {
    before browser.js.sandbox(code) requires user.confirm
  }

  stage RunSandbox {
    exec: browser.js.sandbox(code: code, input: code)
    output: {
      result: json
    }
  }
}
