[package]
name = "nemoir-backend-python"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
nemoir-ir = { path = "../nemoir-ir" }
thiserror = "2"
serde_yaml = "0.9"

[dev-dependencies]
indexmap = { version = "2", features = ["serde"] }
